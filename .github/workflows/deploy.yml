name: Deploy AWS Infrastructure

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: Deploy CloudFormation Stack
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ap-south-1
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}

      - name: Verify AWS identity
        run: |
          echo "AWS identity:"
          aws sts get-caller-identity

      - name: Wait before CloudFormation deployment
        run: |
          echo "Waiting 10 seconds before checking CloudFormation..."
          sleep 10

      - name: Check CloudFormation stack state
        id: check-stack
        shell: bash
        run: |
          STATUS=$(aws cloudformation describe-stacks \
            --stack-name s3-glue-pipeline-infra \
            --region ap-south-1 \
            --query "Stacks[0].StackStatus" \
            --output text 2>/dev/null || echo "DOES_NOT_EXIST")

          echo "Current stack status: $STATUS"
          echo "status=$STATUS" >> "$GITHUB_OUTPUT"

      - name: Delete failed rollback stack
        if: steps.check-stack.outputs.status == 'ROLLBACK_COMPLETE'
        run: |
          echo "Stack is in ROLLBACK_COMPLETE."
          echo "Deleting failed stack before recreating it..."

          aws cloudformation delete-stack \
            --stack-name s3-glue-pipeline-infra \
            --region ap-south-1

          echo "Waiting for stack deletion to complete..."

          aws cloudformation wait stack-delete-complete \
            --stack-name s3-glue-pipeline-infra \
            --region ap-south-1

          echo "Failed stack deleted successfully."

      - name: Validate CloudFormation template
        run: |
          echo "Validating CloudFormation template..."

          aws cloudformation validate-template \
            --template-body file://cloudformation/infrastructure.yaml \
            --region ap-south-1

          echo "CloudFormation template validation successful."

      - name: Deploy CloudFormation stack
        run: |
          echo "Deploying CloudFormation infrastructure..."

          aws cloudformation deploy \
            --template-file cloudformation/infrastructure.yaml \
            --stack-name s3-glue-pipeline-infra \
            --region ap-south-1 \
            --capabilities CAPABILITY_NAMED_IAM \
            --role-arn "${{ secrets.CLOUDFORMATION_ROLE_ARN }}"

          echo "CloudFormation deployment successful."

      - name: Get S3 bucket names
        id: get-buckets
        run: |
          INPUT_BUCKET=$(aws cloudformation describe-stacks \
            --stack-name s3-glue-pipeline-infra \
            --region ap-south-1 \
            --query "Stacks[0].Outputs[?OutputKey=='BucketName'].OutputValue" \
            --output text)

          OUTPUT_BUCKET=$(aws cloudformation describe-stacks \
            --stack-name s3-glue-pipeline-infra \
            --region ap-south-1 \
            --query "Stacks[0].Outputs[?OutputKey=='OutputBucketName'].OutputValue" \
            --output text)

          echo "Input bucket: $INPUT_BUCKET"
          echo "Output bucket: $OUTPUT_BUCKET"

          echo "input_bucket=$INPUT_BUCKET" >> "$GITHUB_OUTPUT"
          echo "output_bucket=$OUTPUT_BUCKET" >> "$GITHUB_OUTPUT"

      - name: Create S3 input and output folders
        run: |
          INPUT_BUCKET="${{ steps.get-buckets.outputs.input_bucket }}"
          OUTPUT_BUCKET="${{ steps.get-buckets.outputs.output_bucket }}"

          echo "Creating input/ folder in input bucket..."
          aws s3api put-object \
            --bucket "$INPUT_BUCKET" \
            --key "input/" \
            --region ap-south-1

          echo "Creating output/ folder in output bucket..."
          aws s3api put-object \
            --bucket "$OUTPUT_BUCKET" \
            --key "output/" \
            --region ap-south-1

          echo "S3 input/ and output/ folders created successfully."

      - name: Upload Glue ETL script
        run: |
          INPUT_BUCKET="${{ steps.get-buckets.outputs.input_bucket }}"

          echo "Uploading Glue ETL script..."
          echo "Source: glue/glue_job.py"
          echo "Destination: s3://$INPUT_BUCKET/glue/glue_job.py"

          aws s3 cp \
            glue/glue_job.py \
            "s3://$INPUT_BUCKET/glue/glue_job.py" \
            --region ap-south-1

          echo "Glue ETL script uploaded successfully."

      - name: Verify Glue ETL script
        run: |
          INPUT_BUCKET="${{ steps.get-buckets.outputs.input_bucket }}"

          echo "Verifying Glue script in S3..."

          aws s3 ls \
            "s3://$INPUT_BUCKET/glue/glue_job.py" \
            --region ap-south-1

          echo "Glue ETL script verified successfully."

      - name: Verify S3 folders
        run: |
          INPUT_BUCKET="${{ steps.get-buckets.outputs.input_bucket }}"
          OUTPUT_BUCKET="${{ steps.get-buckets.outputs.output_bucket }}"

          echo "Verifying input/ folder..."
          aws s3 ls "s3://$INPUT_BUCKET/input/" \
            --region ap-south-1

          echo "Verifying output/ folder..."
          aws s3 ls "s3://$OUTPUT_BUCKET/output/" \
            --region ap-south-1

          echo "S3 folders verified successfully."

      - name: Show stack outputs
        run: |
          echo "========================================="
          echo "CloudFormation Stack Outputs"
          echo "========================================="

          aws cloudformation describe-stacks \
            --stack-name s3-glue-pipeline-infra \
            --region ap-south-1 \
            --query "Stacks[0].Outputs[*].[OutputKey,OutputValue]" \
            --output table

          echo "========================================="
          echo "Deployment completed successfully."
          echo "========================================="